# Vue Client

## Run

1. Start server in repo root:
   - `cargo run -p server`
2. Start Vue client:
   - `cd vue-client`
   - `npm install`
   - `npm run dev`

Default dev URL: `http://127.0.0.1:5173`

## Build

- `npm run build`
- Output directory: `dist/`
